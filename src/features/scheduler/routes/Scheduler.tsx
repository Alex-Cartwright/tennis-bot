import { Button } from "@mui/material"
import { DateCalendar } from "@mui/x-date-pickers"

export const Scheduler = () => {
  return (
    <div>
      <h2>Scheduler</h2>
      <Button>test</Button>
      <DateCalendar />
    </div>
  )
}